/**
Crea un constructor para el objeto Polo 
con parámetros para definir los atributos: marca, color y talle.
 */

function Polo(marca, color, talle){
    this.marca = marca;
    this.color = color;
    this.talle = talle;
}